# طرح محصول: اپلیکیشن مدیریت مالی شخصی (فلاتر)

## ۱. مسئله و چشم‌انداز

وقتی حقوق واریز می‌شود، بخشی از آن از قبل «تعهد شده» است (قسط، اجاره، پرداخت به فرد دیگر و…) ولی چون تاریخ سررسیدش نرسیده، در حساب می‌ماند و باعث می‌شود کاربر فکر کند پول بیشتری در اختیار دارد. این اپ باید:

- **موجودی واقعیِ «آزاد برای خرج کردن»** را نشان بدهد (نه موجودی خام بانک)
- تراکنش‌ها را **خودکار از پیامک بانک** بخواند
- از کاربر **علت هزینه** را بپرسد (دسته‌بندی)
- در پایان **ماه شمسی / دوره حقوقی دلخواه** گزارش بدهد
- حساب هر بانک را **مجزا** مدیریت کند

---

## ۲. نگاشت خواسته‌ها به ویژگی محصول

| # | خواسته شما | ویژگی محصول |
|---|---|---|
| ۱ | برنامه‌ریزی پرداخت‌های آینده و کسر از موجودی نمایشی | ماژول «تعهدات/پرداخت‌های برنامه‌ریزی‌شده» |
| ۲ | اتصال به پیامک و به‌روزرسانی خودکار موجودی | ماژول «خواندن و تفسیر پیامک بانکی» |
| ۳ | پرسیدن علت هر تراکنش (فوری یا در باز شدن بعدی) | ماژول «دسته‌بندی تراکنش» + نوتیفیکیشن |
| ۴ | گزارش پایان ماه شمسی | ماژول «گزارش‌ساز» |
| ۵ | دوره حقوقی سفارشی (مثلاً ۵ تا ۵) | تنظیمات «دوره مالی» |
| ۶,۷,۸ | فارسی، شمسی، ریال با نمایش تومان زیر اینپوت | لایه لوکالایزیشن و فرمت واحد پول |
| ۹ | بدون لاگین | ذخیره‌سازی کاملاً محلی (Local-first) |
| ۱۰ | تفکیک الگوی پیامک بر اساس بانک | «موتور قالب پیامک» با پروفایل هر بانک |
| ۱۱ | حساب مجزا برای هر بانک | مدل چندحسابی (Multi-account) |
| ۱۲ | طراحی شبیه اپ‌های گوگل | Material Design 3 دقیق |

---

## ۳. معماری فنی پیشنهادی

**فریم‌ورک:** Flutter (Android اولویت اول، چون خواندن پیامک در iOS عملاً ممکن نیست)

**State Management:** Riverpod (تست‌پذیر، مناسب برای منطق محاسباتی زیاد)

**دیتابیس محلی:** Drift (SQLite) — چون رابطه‌ای بودن داده‌ها (حساب↔تراکنش↔دسته‌بندی↔تعهد) به schema نیاز دارد

**تقویم شمسی:** پکیج `shamsi_date` + ویجت `persian_datetime_picker`

**نوتیفیکیشن:** `flutter_local_notifications`

**خواندن پیامک:** `telephony` یا `another_telephony` (نیازمند permission ویژه‌ی `READ_SMS` / `RECEIVE_SMS`)

**نمودار گزارش:** `fl_chart`

**فرمت اعداد فارسی/جداکننده هزارگان:** `intl` با locale `fa`

### ⚠️ نکته فنی مهم درباره خواندن پیامک
گوگل‌پلی خواندن خودکار پیامک را فقط برای دسته‌های محدودی (مثل اپ پیش‌فرض SMS) مجاز می‌داند. برای این اپ دو راه وجود دارد:

1. **مسیر ساده و امن (پیشنهادی برای MVP):** کاربر پیامک بانک را **کپی/اشتراک‌گذاری (Share Intent)** می‌کند به اپ، یا از Notification Listener (خواندن نوتیف بانک، نه SMS) استفاده می‌شود — این مسیر محدودیت پلی‌استور کمتری دارد.
2. **مسیر پیشرفته:** خواندن مستقیم SMS با permission — فقط در صورت انتشار خارج از Google Play (نصب مستقیم APK) بدون محدودیت اجرا می‌شود؛ اگر قرار است در Google Play منتشر شود باید توجیه سیاست‌های Google را در نظر گرفت.

پیشنهاد می‌کنم فاز اول را با **Notification Listener** (خواندن نوتیفیکیشن بانک به‌جای SMS) پیاده کنید که هم راحت‌تر تایید می‌شود و هم اکثر بانک‌های ایرانی نوتیف واریز/برداشت می‌فرستند.

---

## ۴. مدل داده‌ای (Schema پیشنهادی)

```
Bank (بانک)
 ├─ id, name, colorHex, icon, smsSenderPattern

Account (حساب)
 ├─ id, bankId, title, currentBalanceRial

Transaction (تراکنش)
 ├─ id, accountId, amountRial, type(deposit/withdraw)
 ├─ rawSmsText, parsedAt, occurredAtJalali
 ├─ categoryId (nullable تا وقتی کاربر دسته‌بندی کند)
 ├─ isCategorized (bool)

Category (دسته‌بندی هزینه)
 ├─ id, title, icon, colorHex, type(income/expense)

PlannedPayment (تعهد/پرداخت آینده)
 ├─ id, accountId, title, amountRial, dueDateJalali
 ├─ status(pending/paid/canceled), categoryId

SalaryPeriod (دوره حقوقی)
 ├─ id, startDayOfMonth (مثلا 5)
 ├─ isActive

SmsTemplate (قالب تفسیر پیامک هر بانک)
 ├─ id, bankId, regexPattern, amountGroup, balanceGroup, typeKeywords
```

---

## ۵. منطق محاسبه «موجودی آزاد»

```
موجودی آزاد = موجودی فعلی حساب
              − مجموع PlannedPayment های pending با dueDate در دوره جاری
```

نمایش پیشنهادی در داشبورد:
- موجودی کل بانک‌ها (رنگ خنثی)
- موجودی آزاد (رنگ سبز/قرمز بسته به مثبت یا منفی بودن)
- جمع تعهدات باقیمانده تا پایان دوره

---

## ۶. صفحات اصلی اپ

1. **داشبورد** — کارت هر بانک، موجودی آزاد کل، هشدار تراکنش‌های دسته‌بندی‌نشده
2. **افزودن/مدیریت بانک‌ها و حساب‌ها**
3. **تعهدات/پرداخت‌های برنامه‌ریزی‌شده** — لیست + افزودن با تاریخ شمسی
4. **تراکنش‌ها** — لیست، فیلتر بر اساس بانک/دسته/بازه زمانی
5. **دسته‌بندی تراکنش** (Bottom Sheet سریع، هم از نوتیف هم از داخل اپ)
6. **گزارش ماهانه** — نمودار دایره‌ای دسته‌بندی هزینه‌ها، مقایسه با ماه قبل
7. **تنظیمات** — دوره حقوقی، مدیریت قالب پیامک بانک‌ها، دسته‌بندی‌های سفارشی

---

## ۷. اصول طراحی (شبیه اپ‌های گوگل)

- Material Design 3 با Dynamic Color (در صورت پشتیبانی دستگاه)
- فونت فارسی مشابه Google Sans مثل **Vazirmatn**
- استفاده از FAB برای افزودن سریع تراکنش/تعهد
- کارت‌های گرد با سایه ملایم (Elevation کم، سبک Material You)
- حالت روشن/تاریک خودکار

---

## ۸. فازبندی پیشنهادی (Roadmap)

- **فاز ۱ (MVP):** ورود دستی تراکنش + تعهدات + گزارش ماهانه + شمسی/ریال-تومان
- **فاز ۲:** خواندن نوتیفیکیشن بانک به‌صورت نیمه‌خودکار
- **فاز ۳:** موتور قالب پیامک برای چند بانک + دسته‌بندی هوشمند پیشنهادی
- **فاز ۴:** بک‌آپ/ریستور محلی (فایل export/import چون لاگین نداریم)

---

# ✅ TODO List برای پیاده‌سازی (برای Anti-Gravity)

```markdown
## Setup
- [ ] ایجاد پروژه Flutter با نام fa_finance (یا نامی که کاربر تعیین می‌کند)
- [ ] افزودن پکیج‌ها: riverpod, drift, shamsi_date, persian_datetime_picker,
      fl_chart, flutter_local_notifications, intl, google_fonts (Vazirmatn)
- [ ] تنظیم locale پیش‌فرض روی fa و جهت RTL
- [ ] تنظیم تم Material 3 با رنگ اصلی برند و فونت Vazirmatn

## دیتابیس (Drift)
- [ ] تعریف جدول Bank
- [ ] تعریف جدول Account
- [ ] تعریف جدول Transaction
- [ ] تعریف جدول Category (با چند دسته پیش‌فرض: خوراک، حمل‌ونقل، قبض، خرید، سایر)
- [ ] تعریف جدول PlannedPayment
- [ ] تعریف جدول SalaryPeriod
- [ ] تعریف جدول SmsTemplate
- [ ] نوشتن DAO برای هر جدول + متدهای CRUD

## منطق محاسباتی
- [ ] سرویس محاسبه موجودی آزاد (کسر تعهدات pending از موجودی)
- [ ] سرویس تشخیص دوره حقوقی جاری بر اساس تاریخ شمسی امروز و startDayOfMonth
- [ ] سرویس تبدیل ریال↔تومان برای نمایش

## UI - داشبورد
- [ ] کارت خلاصه موجودی آزاد کل
- [ ] لیست کارت بانک‌ها با موجودی هرکدام
- [ ] بخش هشدار «تراکنش‌های دسته‌بندی‌نشده»
- [ ] FAB برای افزودن سریع تراکنش/تعهد

## UI - بانک‌ها و حساب‌ها
- [ ] فرم افزودن بانک (نام، رنگ، آیکون)
- [ ] فرم افزودن حساب به یک بانک با موجودی اولیه

## UI - تعهدات (Planned Payments)
- [ ] فرم افزودن تعهد: عنوان، مبلغ (ریال با نمایش تومان)، تاریخ شمسی سررسید، حساب مرتبط
- [ ] لیست تعهدات با وضعیت (در انتظار/پرداخت‌شده)
- [ ] دکمه «علامت‌گذاری به‌عنوان پرداخت‌شده» که یک Transaction واقعی می‌سازد

## UI - تراکنش‌ها
- [ ] لیست تراکنش‌ها با فیلتر بانک/دسته/بازه تاریخ شمسی
- [ ] فرم افزودن تراکنش دستی
- [ ] Bottom Sheet دسته‌بندی سریع تراکنش

## پیامک/نوتیفیکیشن بانکی
- [ ] پیاده‌سازی Notification Listener (خواندن عنوان/متن نوتیف بانک)
- [ ] رابط تعریف قالب هر بانک (regex ساده برای استخراج مبلغ و نوع تراکنش)
- [ ] هنگام دریافت پیامک/نوتیف: ساخت Transaction با isCategorized=false
- [ ] ارسال نوتیفیکیشن محلی «این هزینه بابت چی بود؟» با دکمه‌های دسته‌بندی سریع
- [ ] در باز شدن اپ: نمایش صف تراکنش‌های دسته‌بندی‌نشده

## گزارش‌ساز
- [ ] محاسبه جمع درآمد/هزینه بر اساس دوره حقوقی فعال
- [ ] نمودار دایره‌ای (fl_chart) تفکیک هزینه به تفکیک دسته‌بندی
- [ ] نمودار مقایسه‌ای هزینه دوره جاری با دوره قبل
- [ ] صفحه گزارش با انتخاب دوره از تقویم شمسی

## تنظیمات
- [ ] فرم تعریف/ویرایش دوره حقوقی (روز شروع ماه)
- [ ] مدیریت دسته‌بندی‌های سفارشی (افزودن/ویرایش/حذف)
- [ ] مدیریت قالب‌های پیامک بانک‌ها
- [ ] Export/Import دیتابیس به فایل (چون لاگین نداریم، این جایگزین بک‌آپ ابری است)

## پرداخت و مانده نهایی
- [ ] تست کامل سناریو: واریز حقوق → ثبت چند تعهد → مشاهده موجودی آزاد صحیح
- [ ] تست دسته‌بندی تراکنش از نوتیفیکیشن
- [ ] تست گزارش پایان دوره حقوقی سفارشی
```
